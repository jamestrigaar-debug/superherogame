import {int} from './rng.js';\nexport function battleRound(player,enemy,rng,card){const enemyDamage=int(rng,5,13),playerDamage=card.damage+Math.floor(player.strength/12);if(card.speed>=enemy.speed){enemy.health-=playerDamage; if(enemy.health>0)player.health-=enemyDamage}else{player.health-=enemyDamage; if(player.health>0)enemy.health-=playerDamage}return enemy.health<=0?'victory':player.health<=0?'defeat':'continue'}\nexport const enemyFor=(tier='F')=>({name:'ROGUE ECHO',health:38+tier.charCodeAt(0)*2,speed:6,damage:8});\n