import {mulberry32,hashSeed,int} from './rng.js';\nimport {createCharacter,alignment,shiftMorality,manifest} from './character.js';\nimport {drawCard,applyEffect} from './cards.js';\nimport {createMap} from './map.js';\nconst $=id=>document.getElementById(id);\nlet seed=hashSeed($('seedInput').value),rng=mulberry32(seed),character=createCharacter(seed),map=createMap(seed),phase='childhood',pending=null;\nconst canvas=$('board'),ctx=canvas.getContext('2d');\nfunction log(text,accent=''){const el=document.createElement('div');el.className='log-entry';el.innerHTML=`<time>TURN ${String(character.turn).padStart(2,'0')} / AGE ${String(character.age).padStart(2,'0')}</time>${accent?`<strong>${accent}</strong> `:''}${text}`;$('log').prepend(el)}\nfunction phaseName(){return character.age<13?'CHILDHOOD':character.age<19?'ACADEMY':'WORLD MAP'}\nfunction update(){const c=character,p=phaseName();$('charName').textContent=c.name;$('healthText').textContent=`${Math.max(0,c.health)} / ${c.maxHealth}`;$('healthBar').style.width=Math.max(0,c.health/c.maxHealth*100)+'%';[['strengthText','strengthBar','strength'],['intText','intBar','intelligence'],['willText','willBar','willpower']].forEach(([t,b,k])=>{$(t).textContent=c[k];$(b).style.width=Math.min(100,c[k]*2)+'%' });$('moralityText').textContent=(c.morality>0?'+':'')+c.morality;$('moralMarker').style.left=(c.morality+100)/2+'%';$('alignText').textContent=alignment(c);$('powerTier').textContent=c.powerTier?`POWER TIER ${c.powerTier}`:'POWER DORMANT';$('traitList').innerHTML=c.traits.length?c.traits.map(t=>`<span class="trait">${t}</span>`).join(''):'<span class="muted">Your story is still forming.</span>';$('ageText').textContent=`AGE ${String(c.age).padStart(2,'0')}`;$('phaseChip').innerHTML=`${p} <b>${p==='CHILDHOOD'?'AGE 5–12':p==='ACADEMY'?'AGE 14–18':'ADULTHOOD'}</b>`;$('kicker').textContent=`CHAPTER ${p==='CHILDHOOD'?'01':p==='ACADEMY'?'02':'03'} / ${c.powerTier?'THE AWAKENED':'BEFORE THE SPARK'}`;$('chapterTitle').textContent=p==='CHILDHOOD'?'Small Moments, Big Consequences':p==='ACADEMY'?'Learn What You Are':'The City Is Your Board';$('turnText').textContent=`TURN ${String(c.turn).padStart(2,'0')}`;$('wealthText').textContent='$'+c.wealth;$('fateText').textContent=c.fate>=0?'+'+c.fate:c.fate;$('diceLabel').textContent=(c.age<12?'D4 · 1–4 STEPS':c.age<18?'D6 · 1–6 STEPS':'D8 · 1–8 STEPS');drawBoard();localStorage.setItem('awakening-save',JSON.stringify({seed,character}))}\nfunction drawBoard(){const w=canvas.width,h=canvas.height;ctx.clearRect(0,0,w,h);ctx.fillStyle='#0c171d';ctx.fillRect(0,0,w,h);ctx.strokeStyle='#152d35';ctx.lineWidth=1;for(let x=0;x<w;x+=38){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,h);ctx.stroke()}for(let y=0;y<h;y+=38){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(w,y);ctx.stroke()}const pts=map.map((n,i)=>({x:n.x+80,y:n.y}));ctx.beginPath();pts.forEach((p,i)=>i?ctx.lineTo(p.x,p.y):ctx.moveTo(p.x,p.y));ctx.strokeStyle='#315b64';ctx.lineWidth=3;ctx.stroke();pts.forEach((p,i)=>{ctx.beginPath();ctx.arc(p.x,p.y,i===character.location?15:9,0,Math.PI*2);ctx.fillStyle=i===character.location?'#52e0e6':i>3?'#ff704755':'#1a3a43';ctx.fill();ctx.strokeStyle=i===character.location?'#bafafa':'#47737b';ctx.stroke();ctx.fillStyle='#a8bdc1';ctx.font='11px Space Mono';ctx.fillText(map[i].name,p.x-35,p.y+30)});const cur=pts[character.location];if(cur){ctx.beginPath();ctx.arc(cur.x,cur.y,24+Math.sin(Date.now()/300)*3,0,Math.PI*2);ctx.strokeStyle='#52e0e688';ctx.stroke()}$('locationText').textContent=map[character.location]?.name||'UNKNOWN'}\nfunction openEvent(card,roll){pending=card;$('eventType').textContent=`EVENT // ${String(character.turn).padStart(2,'0')}`;$('eventFate').textContent=`FATE ${roll.fate>=0?'+':''}${roll.fate}`;$('eventArt').textContent=card.art;$('eventKicker').textContent=card.kicker;$('eventTitle').textContent=card.title;$('eventText').textContent=card.text;$('choices').innerHTML=card.choices.map((c,i)=>`<button class="choice" data-i="${i}"><span>${c.label}</span><small>${c.tag}</small></button>`).join('');$('outcome').classList.add('hidden');$('continueBtn').classList.add('hidden');$('eventModal').classList.remove('hidden');document.querySelectorAll('.choice').forEach(b=>b.onclick=()=>choose(+b.dataset.i))}\nfunction choose(i){const choice=pending.choices[i];const effects=choice.effect(character);applyEffect(character,effects);if(effects.potential){character.traits.push(choice.tag.replace('+ ','').replaceAll(' ','_'))}if(character.age>=12&&!character.powerTier){manifest(character,seed);character.traits.push('AWAKENED');log(`Manifestation event. Power tier ${character.powerTier}.`,'THE SPARK');}$('choices').classList.add('hidden');$('outcome').textContent=choice.result;$('outcome').classList.remove('hidden');$('continueBtn').classList.remove('hidden');update()}\n$('continueBtn').onclick=()=>{$('eventModal').classList.add('hidden');$('choices').classList.remove('hidden');$('rollBtn').disabled=false;$('turnSummary').textContent='The next moment is yours to shape.'};\n$('rollBtn').onclick=()=>{if(character.health<=0)return;const sides=character.age<12?4:character.age<18?6:8,steps=int(rng,1,sides);character.fate=steps===1?-1:steps===sides?1:0;character.turn++;character.location=Math.min(map.length-1,character.location+steps);if(character.turn%2===0)character.age++;if(character.age===12&&!character.powerTier)character.age=12;const card=drawCard(character);$('rollBtn').disabled=true;$('turnSummary').textContent=`Moved ${steps} space${steps>1?'s':''}. A moment finds you.`;log(`Moved to ${map[character.location].name}.`, 'MOVE');openEvent(card,{fate:character.fate});update()};\n$('newSeed').onclick=()=>{$('seedInput').value=Math.floor(Math.random()*999999);reset()};$('resetBtn').onclick=reset;$('saveBtn').onclick=()=>{localStorage.setItem('awakening-save',JSON.stringify({seed,character}));toast('RUN SAVED TO THIS DEVICE')};function reset(){seed=hashSeed($('seedInput').value);rng=mulberry32(seed);character=createCharacter(seed);map=createMap(seed);$('log').innerHTML='';log('A new life begins. The variables are in motion.','SYSTEM');update()}function toast(t){$('toast').textContent=t;$('toast').classList.add('show');setTimeout(()=>$('toast').classList.remove('show'),1800)}log('A new life begins. The variables are in motion.','SYSTEM');update();window.addEventListener('resize',drawBoard);\n